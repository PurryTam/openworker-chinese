// Test setup: initialize the i18n instance so components using `useTranslation()` render with
// real (English, interpolated) text instead of raw `{{var}}` keys. Mirrors main.tsx's import.
import "./src/i18n";
