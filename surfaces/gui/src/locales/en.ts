// English bundle. Per the key=English strategy (see ../i18n.ts), the English UI text IS the
// translation key, so this bundle is intentionally empty: t("Open at login") resolves to the
// key itself for English users. Only the zh-Hant bundle below carries real mappings.
const en: Record<string, string> = {};

export default en;
